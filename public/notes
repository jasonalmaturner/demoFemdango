To do now:



home
    favorites gallery
   * (badges explained)
   * recent reviews
    
   * logout toggle
    
about
    content 
        femdango
        review methods
        resources
    
    * contact go live!

reviews
    * show review form when logged in
    * submit reviews
    * show reviews on movie page
    * show recent reviews on home page
    
    tally badge % and reveal top badge count % in slider 
    
dashboard
    * add favorites array 
    add slider
     - hover to reveal remove icon
     - ng-change to add from movie page
    
   * add wishlist array 
    add slider
     - hover to reveal remove icon
     - hover to reveal add to favorites icon
     -ng-change to add from movie page
     
     
Buy tickets!
    
    
*ng-click scope.showspinner= true
*resolve 
*scope.showspinner = false
      
      
    data!
    

   movie: {
   id,     
   badgesClickedPercent: {
   "bechdelPercent" : = (timesClicked/timesReviewed), 
   "strongPercent" : 0, 
   "MakoPercent" : 0,
   "execPercent" : 0,
   "lampPercent" : 0,
   "genderRatioPercent" : 0,
   "lgbtPercent" : 0,
   "pocPercent" : 0,
   "diversityPercent" : 0;
   "disabilitiesPercent": 0;
   }
   
   
   user: {
   uid,
   email:,
   password:,
   wishlist: [movie.id, movie.id ...],
   favorites:[movie.id, movie.id ...],
   }
   
   review: {
   createdAd: ,
   user.uid,
   text:"review text",
   badges: "0000000000"
   }
   var badges= {
   "bechdel" : 0, 
    "strong" : 1, 
   "Mako" : 2,
   "exec" : 3,
   "lamp" : 4,
   "genderRatio" : 5,
   "lgbt" : 6,
   "poc" : 7,
   "diversity" : 8;
   "disabilities": 8;
assigns binary to badges at the index
}
To do eventually:

index
    add info to footer

home
    add galleries
        new movies (move from browse page)
        user favorites
        most badges
        
    add sdk from 
        amazon
        fandango 
        netflix
     preofessional reviews
     links to articles



about
x    ng-show() to toggle between views
    content
        femdango bio
        bechdel info
        resources
    contact form function

browse
x    bechdel api
 xx   fandango api ---> TMDb api and Omdb
    make slick wait for api to load? Why does it work on refresh?
    replace image with title if image isn't available 
    Get IMDB ID for TMDB API
    link TMDB API to bechdel!!!!!!!!!!
    insert bechdel rating
    change class if bechdel passes
    stop slick on hover;
    
    
    
Movie page
   * link "searchMovie" function to page data on load, synch. 
   * recent comments box
   * nominate for badges
    ng-click so wish List/favorites values are saved for the user. 
    pause slick when active link
    
    
* sign in 
    set up authentication
    create comment section
    create favorites list
    create wishlist
    
  
http://www.myapifilms.com/index.jsp
-imdb id

-actors
-actor/actress ***
-actor trivia ***
-actor bio ***
-personal quotes, etc ***
-actor photos ***
-filmography ***
-actor trademark ***

-awards details ***
-photos
-trailers ***
-plot short
-plot long ***

-similar movies
-top movies
-in theaters
-coming soon
-popular trailers

www.omdbapi
-tomato data
-website

tmdbapi
-search***
-search multi***
-box office



bechdelapi
bechdeltext
dvd release

  
  
    
    

recursion
function logger(x){
if(!x) {x = 0;
consol.log(x)};
if(x > 6) {return x}
else {return logger(x+1)}
}
functin();

else returns 7 to the scope above it continually until it reaches the global scope. 
var arr = [1,2,3,4,5,6]
function sum(arr){
var sum = 0;
    for(var i = 0; i < arr.length; i++) {
    sum += arr[i]; // sum = sum + arr[i]
    }
    return sum
    }
    sum(arr);
    
function arrAdd(arr, sum, index){
if(!sum) sum = 0;
if(!index) index = 0;
if(index === arr.length) {return sum};
else return summary(arr, sum += arr[index], index + 1)
}
arrAdd(arr);



